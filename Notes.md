int index = myStr1.find(myStr2, from_index)
bool ans = ((myStr1 == myStr2);
myStr.insert(index, str/char);
int freq = count(myStr.begin(),myStr.end() ,char);
string rr_S = S.substr( N-K, K) + S.substr(0,N-K)   Right Rotation
string Lr_S.substr(K,N-K) + S.substr(0,K)  Left Rotation
